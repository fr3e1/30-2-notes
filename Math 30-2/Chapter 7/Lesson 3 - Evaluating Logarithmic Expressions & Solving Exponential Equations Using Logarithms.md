
# Notes
For every operation, there is an inverse operation that undoes the operation; For every function, there is an inverse function that undoes the function.

Recall: Logarithmic functions are inverses of exponential functions (meaning we switch the x and the y in the equation).

given: $$y = b^x$$
switch x and y:$$x = b^y$$
isolate for y: $$y = \log_{b}x$$
example: $$x = 2^y \to y = \log_{2}x$$
notice: the bases in the exponential equation are the **bases** for the logarithmic equation. 
"Base stays the same and the other two parts switch"

Since the logarithmic function y = log_{b}x is only defined for positive values of x, the logarithm of a negative number cannot be determined

the logarithmic equation y = log_{b}x can be expresses in exponential form as x = b^y
the exponential equation y = b^x can be expressed in the logarithmic form as log_{b}y = x

# examples
## example 1
a) 
given: $$64 = 4^3$$
ans: $$3 = \log_{4}64$$
b)
given: $$\frac{1}{8} = 2^{-3}$$
ans: $$-3 = \log_{2}\frac{1}{8}$$

c)
given: $$100 = 10^2$$
answer: $$2 = \log_{10}100$$

d)
given: $$5 = x^{2y}$$
answer: $$\log _{x}5=2y$$

e)
given:$$e^d = f$$
answer:$$\log_{d}e = f$$
## example 2
a) $$7^4 = x$$
b)$$10^{3} = 1000$$
c) $$t^m = b$$
d)$$x ^{b^4}= a$$
e) $$\frac{5}{4}=\log_{b}6$$
$$b^{\frac{5}{4}} = 6$$
## example 3

a) $\log_{10}81 = x$, $x = 1.91$

b)$\log_{5}8.32=x$, $x = 0.18$

c)$\log_{6}12 = x$, $x = 1.39$


$## example 4

a) $5^3 = x$, $x = 25$

b)$3^4=3x$, $x = 27$

c) $2^{\frac{4}{3}} = x$, $x = 2.52$

inverses logs bases switch $y = x^x \to x = \log_{a}y$ 

Exponential functions and logarithmic functions are **inverses** of each other. 

● If we have an unknown exponent and we cannot make the bases equal, then we must use **logs**. 

● To switch between exponential form and logarithmic form, we keep the **bases** the same and **switch** the other two (or look at the formula sheet). 

● Our calculator uses base _______ when working with logs. To change the base, we must use the  (unless your calculator has a fancy log to multiple base capability). 

● We can never take the log of a _________________________ number. 

● ______ $log _{a}a = 1$ *this is not on the formula sheet, however it is useful to know.
ex.: $\log_{3}3 = 1$

# Practice
### reminder:  $x = a^y \to y = \log_{a}x$
1.
a) $5^x = 36 \to \log_{5}36 =x$
b) $3^{(x+2)}=18 \to \log(3)18=x+2$
c) $5*3^{(x-1)}=20\to \log_{15}20=x-1$

2.
a)$\log_{5}x=8\to5^8=x$
b)$\log_{2}(x-1)=3\to{2}^3=x-1$
c)$3\log_{8}x=7\to{8}^{\frac{7}{3}}=x$
